{
  "name": "pretty-number-machine",
  "version": "1.0.0-dev.7",
  "private": true,
  "description": "An interactive visualisation of prime factorisation, where a number's colour is its factorisation.",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node tools/check.mjs && node tools/check-achievements.mjs",
    "sync": "cap sync android",
    "android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
